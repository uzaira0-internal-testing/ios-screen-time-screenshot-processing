# Frontend Development Dockerfile
# Runs Vite dev server with hot module replacement (HMR)

FROM oven/bun:1-alpine

WORKDIR /app

# Install dependencies first for better caching
# --ignore-scripts skips native module compilation (better-sqlite3 not needed for dev server)
COPY frontend/package.json frontend/bun.lock ./
RUN bun install --ignore-scripts

# Note: Source code is mounted as a volume in docker-compose.dev.yml
# This enables hot reloading - changes to source files are immediately reflected

# Expose Vite dev server port
EXPOSE 5173

# Environment variables for development
ENV NODE_ENV=development

# Start Vite dev server with host binding for Docker
# --host exposes the server to the Docker network
CMD ["bun", "run", "dev", "--", "--host", "0.0.0.0"]
